# Frontend (Vite) — optional.
# In dev, requests to /api are proxied to the backend (see vite.config.js),
# so you normally don't need to set this. For production, point it at your API.
# VITE_API_BASE=/api
